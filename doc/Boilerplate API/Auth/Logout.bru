meta {
  name: Logout
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: bearer
}

headers {
  {{clientKey}}: {{clientSecret}}
}

auth:bearer {
  token: {{accessToken}}
}
