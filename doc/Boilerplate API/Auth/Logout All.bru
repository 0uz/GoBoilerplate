meta {
  name: Logout All
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: bearer
}

headers {
  {{clientKey}}: {{clientSecret}}
}

auth:bearer {
  token: {{accessToken}}
}
